import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.imobeddyrealestatecrm',
  appName: 'imobeddy---real-estate-crm',
  webDir: 'dist'
};

export default config;
